// Seu JavaScript
window.addEventListener("load", function () {
    // Após 4 segundos, adiciona a classe "hidden" ao elemento com o id "loader"
    setTimeout(function () {
      document.getElementById("loader").classList.add("hidden");
    }, 4000);
  });
  
